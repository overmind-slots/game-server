package org.overmind.slots.gameserver

private fun createSymbol(number: Int): Symbol {
    return Symbol(number, "Symbol$number")
}

val s0 = createSymbol(0)

val s1 = createSymbol(1)

val s2 = createSymbol(2)

val s3 = createSymbol(3)
